version: '3.8'
services:
  malware_detector:
    image: malware-detector:latest
    ports:
      - '8000:8000'
    environment:
      - ENV=production
    volumes:
      - ./data:/app/data
    networks:
      - malware-net

networks:
  malware-net:
    driver: bridge
